class Mining:
    def __init__(self, userId, iterationId, totalPoints, totalStories, owner):
        self.userId = userId
        self.iterationId = iterationId
        self.totalPoints = totalPoints
        self.totalStories = totalStories
        self.name = owner
