

interface interface1 {
    storyType: string,
    totalStories: number
    totalPoints: number
}

interface interface2 {
    iteration: string,
    totalStories: number
}

export interface velocity {
    id: number,
    velocity: number,
    capacity: number
}


interface dataInterface {
    storyType: string,
    totalStories: number,
    totalPoints: number,
    iteration: string
}